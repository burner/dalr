S := C; 

C := C IF; {: ret = buildTreeWithLoc(termC, [-2, -1], actionNum, this.tokenStack[-2].getLoc()); :}
	| IF;

IF := if B then IF; {: ret = buildTreeWithLoc(termIF, [-3,-1], actionNum, this.tokenStack[-4].getLoc()); :}
	| if B then IF else IF; {: ret = buildTreeWithLoc(termIF, [-5,-3,-1], actionNum, this.tokenStack[-6].getLoc()); :}
	| ID ;

ID := identifier; {: ret = buildTree(termID, [-1], actionNum); :}

B := bool ; {: ret = buildTree(termB, [-1], actionNum); :}
