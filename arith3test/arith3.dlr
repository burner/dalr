S := C; 

C := C A; {: ret = buildTreeWithLoc(termC, [-2, -1], actionNum, this.tokenStack[-2].getLoc()); :}
	| A;

A := if B then A; {: ret = buildTreeWithLoc(termA, [-3,-1], actionNum, this.tokenStack[-4].getLoc()); :}
	| if B then A else A; {: ret = buildTreeWithLoc(termA, [-5,-3,-1], actionNum, this.tokenStack[-6].getLoc()); :}
	| identifier ;

B := bool ;
